# For information about entries in this file, type "man shorewall-policy"
###############################################################################
#SOURCE     DEST        POLICY      LOG LEVEL   LIMIT:BURST
$FW     all     ACCEPT
{% for i in policies %}
{{ i.src }}	{{ i.dst }}	{{ i.policy }}
{% endfor %}

# The FOLLOWING POLICY MUST BE LAST
all     all     REJECT
